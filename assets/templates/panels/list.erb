<div class="list">
	<h1><%= title %></h1>
	<% if list.empty? %>
		<span class="empty"><%= empty_text %></span>
	<% else %>
		<ul>
			<% list.each do |text| %>
				<li><%= text %></span></li>
			<% end %>
		</ul>
	<% end %>
</div>
